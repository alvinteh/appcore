define(function(require) {
    "use strict";

    var Event = require("../../core/models/event");

    /*
        @abstract
        @class Event

        @classdesc Event class.
    */

    return Event;
});